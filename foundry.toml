[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.19"
fs_permissions = [{ access = "read-write", path = "./deploy"}]

[rpc_endpoints]
mainnet = "${MAINNET_RPC_URL}"
sepolia = "${SEPOLIA_RPC_URL}"
